import React from "react";
import { View, Image, StyleSheet, Text, Dimensions } from "react-native";

export default function Template() {
  return <View style={styles.template}></View>;
}

const styles = StyleSheet.create({
  template: {},
});
